/// <reference types='jest' />
import ServiceListStore from "../serviceListStore";

describe('editServiceFormStore', () => {
  describe('Default', () => {
    it('should return all the available roles from services', () => {

    });
  });
});